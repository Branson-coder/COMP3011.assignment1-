# Local development overrides.
# Nothing OpenAI-related lives here: export OPENAI_API_KEY in your shell
# before running, e.g. (bash/zsh):
#   export OPENAI_API_KEY=sk-...
#   mvn spring-boot:run
# or (PowerShell):
#   $env:OPENAI_API_KEY="sk-..."
#   mvn spring-boot:run
#
# This file exists purely to demonstrate profile-based config separation
# between local and TITAN runs, per the assignment's engineering practice
# requirement - it currently has no local-specific overrides needed.
logging.level.com.adelaide.sttapp=DEBUG
